name: 'ci-rickroll'
description: 'Rickroll when CI fails on PRs or pushes'
inputs:
  percentage:
    description: 'Chance (0–100) to rickroll'
    required: false
    default: '100'
  message:
    description: 'Fallback message if random roll does not trigger'
    required: false
    default: ''
  GITHUB_TOKEN:
    description: 'GitHub token (usually secrets.GITHUB_TOKEN)'
    required: true
runs:
  using: 'node20'
  main: 'index.js'
